# @package _global_.model
name: "chronos2"

weights_path: "/home/d32485/timetensor/src/timetensor/sota/chronos2/weights"
device_map: "cuda"
torch_dtype: "bfloat16"

batch_size: 32
num_samples: 20

# Probabilistic output: predict_quantiles() returns these levels
probabilistic: true
quantile_levels: [0.1, 0.5, 0.9]

# Season length for MASE (match your dataset's periodicity)
# Daily cycle @ 30-min steps = 48; hourly = 24
season_length: 48

include_mape: false
