# @package _global_.model
name: "tsicl"
weights_path: "/home/d32485/checkpoints/tsicl-v1.ckpt"   # ← adapte au chemin réel
device: "cuda"

batch_size: 32
point_estimator: "median"    # ou "mean"

probabilistic: false
quantile_levels: [0.1, 0.5, 0.9]

season_length: 48
include_mape: false