defaults:
  - dataset: cer_bis
  - model: prophet
  - _self_

output_dir: "."
hydra:
  run:
    dir: outputs/prophet/${dataset.name}/${now:%Y-%m-%d_%H-%M-%S}
  sweep:
    dir: outputs/prophet/${dataset.name}/multirun_${now:%Y-%m-%d_%H-%M-%S}
    subdir: ctx${dataset.context_length}_h${dataset.prediction_length}

eval_split: "test"  