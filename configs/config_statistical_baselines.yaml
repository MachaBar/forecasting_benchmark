defaults:
  - dataset: cer
  - model: statistical
  - _self_

output_dir: "outputs/statistical"

hydra:
  run:
    dir: outputs/statistical/${dataset.name}/${model.max_lookback}/${now:%Y-%m-%d_%H-%M-%S}
